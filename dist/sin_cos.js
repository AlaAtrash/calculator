export class Trigo {
    constructor(angle) {
        this.angleNumber = angle;
    }
    ;
    sinus() {
        return Math.sin(this.angleNumber);
    }
    cosinus() {
        return Math.cos(this.angleNumber);
    }
}
